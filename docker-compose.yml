services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080/api}
        VITE_APP_NAME: ${VITE_APP_NAME:-Crossfit Gaymes}
    ports:
      - "8081:80"
    restart: unless-stopped
